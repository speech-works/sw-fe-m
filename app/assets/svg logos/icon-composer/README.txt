iOS 26 Icon Composer layers.
Import both SVGs into Icon Composer (File > New, then drag layers in).
layer-2-foreground.svg goes ABOVE layer-1-background.svg.
Deliberately contains NO baked shadows, highlights or bevels - Icon Composer
generates specular, blur and shadow itself for Default/Dark/Clear/Tinted modes.
Do NOT add a rounded-rect mask; the platform applies it.
